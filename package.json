{
  "name": "it-ticket-management",
  "version": "0.1.0",
  "private": true,
  "description": "ITSM MVP skeleton aligned with analysis and design reports",
  "packageManager": "pnpm@10.6.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
