<?xml version="1.0" encoding="UTF-8"?>
  <Configuration status="WARN" monitorInterval="30">
      <Appenders>
          <Console name="Console" target="SYSTEM_OUT">
              <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [traceId=%X{traceId}] [ticketId=%X{ticketId}] [actor=%X{actor}] [%t] %c{1} - %msg%n"/>
          </Console>

          <Kafka name="Kafka" topic="itsm.logs" syncSend="false">
              <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] %c{1} - %msg%n"/>
              <Property name="bootstrap.servers">${env:SPRING_KAFKA_BOOTSTRAP_SERVERS:-localhost:9092}</Property>
          </Kafka>
      </Appenders>

      <Loggers>
          <Logger name="org.apache.kafka" level="WARN" additivity="false">
              <AppenderRef ref="Console"/>
          </Logger>

          <Root level="INFO">
              <AppenderRef ref="Console"/>
              <AppenderRef ref="Kafka"/>
          </Root>
      </Loggers>
  </Configuration>
