package com.itsm.ticket.ticket.domain.enums;

public enum ResolutionCode {
    FIXED,
    WORKAROUND,
    USER_ERROR,
    CONFIGURATION_CHANGE,
    KNOWN_ERROR,
    NOT_REPRODUCIBLE,
    DUPLICATE,
    NO_ACTION_REQUIRED
}
