package com.itsm.ticket.ticket.domain.enums;

/** Manager-approval state of a {@link com.itsm.ticket.ticket.domain.ServiceRequestTicket}. */
public enum ServiceRequstApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED
}
