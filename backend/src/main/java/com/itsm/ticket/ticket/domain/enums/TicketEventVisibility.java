package com.itsm.ticket.ticket.domain.enums;

/** Visibility scope for timeline events and attachments. EXTERNAL is customer-visible. */
public enum TicketEventVisibility {
    EXTERNAL,
    INTERNAL
}
